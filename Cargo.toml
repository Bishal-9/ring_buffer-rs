[package]
name = "ring_buffer"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
cfg-if = "1.0"
loom = { version = "0.7.2", optional = true }

[features]
default = []
loom_test = ["loom"]
